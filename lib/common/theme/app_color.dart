import 'dart:ui';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF1B7F79); // Teal/Green from cards
  static const Color primaryDark = Color(0xFF0F5B56);
  static const Color primaryLight = Color(0xFF4A9F9A);

  // Accent Colors
  static const Color accent = Color(0xFFFFC542); // Yellow from button
  static const Color accentDark = Color(0xFFE6A000);

  // Background Colors
  static const Color background = Color(0xFFf0f1ec); // Light grey background
  static const Color cardBackground = Color(0xFFFFFFFF);
  static const Color sidebarBackground = Color(0xFFF8F8F8);

  // Text Colors
  static const Color textPrimary = Color(0xFF2D2D2D);
  static const Color textSecondary = Color(0xFF6B6B6B);
  static const Color textLight = Color(0xFF9E9E9E);

  // Status Colors
  static const Color ready = Color(0xFF4CAF50);
  static const Color inProgress = Color(0xFFFF9800);
  static const Color completed = Color(0xFF2196F3);
  static const Color waiting = Color(0xFF9C27B0);

  // Badge/Avatar Colors
  static const Color tealBadge = Color(0xFF1B7F79);
  static const Color yellowBadge = Color(0xFFFDB44B);
  static const Color blueBadge = Color(0xFF2196F3);

  // Border & Divider
  static const Color border = Color(0xFFE0E0E0);
  static const Color divider = Color(0xFFEEEEEE);

  // Shadows
  static const Color shadow = Color(0x1A000000);
}
